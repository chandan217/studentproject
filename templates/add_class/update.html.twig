{% extends 'base.html.twig' %}
{% block body %}
{% if form is defined %}
    <div class="container">
           <div class="row">
               <div class="col-sm-4">
                   <h2 class=page-header>Update Class</h2>
                       {{form_start(form)}}
                       {{form_widget(form)}}
                       {{form_end(form)}}
               </div>
           </div>
    </div>
    {% endif %}
<div class="container">
	<br>  
<table class="table">
  <tbody>
  {% for data in addclass %}
    <tr>
      <th scope="row">{{data.id}}</th>
      <td>{{data.Class}}</td>
      <td>{{data.id}}</td>
       </tr>
    {% endfor %}
  </tbody>
</table>
</div>
{% endblock %}